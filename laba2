#include <stdio.h>
main()
{
	int k;
	float S,exp,T;
	printf("write quantity N:");
	scanf("%f",&exp);
	S = 0.0;
 	T=0.25;
	for (k = 3; T > exp; k++)
	{
		T = (k-1) / ((k + 1.0)*(k + 1.0));
		S = S + T;
	}
	printf("summ is:%f\n", S);
	system("pause");
	return 0;
}
